import pool from "../config/db.js";
async function ensureNotificationsTable(){await pool.query(`CREATE TABLE IF NOT EXISTS notifications (notification_id INT AUTO_INCREMENT PRIMARY KEY,type VARCHAR(30) NOT NULL,title VARCHAR(255) NOT NULL,message TEXT NOT NULL,employee_id INT NULL,employee_name VARCHAR(255) NULL,payload JSON NULL,is_read TINYINT(1) NOT NULL DEFAULT 0,created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,INDEX idx_notifications_created (created_at),INDEX idx_notifications_read (is_read),INDEX idx_notifications_employee (employee_id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`)}
export async function getNotifications(_req,res){try{await ensureNotificationsTable();const[rows]=await pool.query(`SELECT notification_id AS id,type,title,message,employee_id AS employeeId,employee_name AS employeeName,payload,is_read AS isRead,created_at AS createdAt FROM notifications ORDER BY created_at DESC LIMIT 100`);res.json({notifications:rows})}catch(error){console.error("Notifications load failed:",error);res.status(500).json({message:"Unable to load notifications"})}}
export async function getMyNotifications(req,res){try{await ensureNotificationsTable();const employeeId=Number(req.user?.employeeId);if(!Number.isInteger(employeeId)||employeeId<=0)return res.status(403).json({message:"A valid employee account is required"});const[rows]=await pool.query(`SELECT notification_id AS id,type,title,message,employee_id AS employeeId,employee_name AS employeeName,payload,is_read AS isRead,created_at AS createdAt FROM notifications WHERE employee_id=? ORDER BY created_at DESC LIMIT 100`,[employeeId]);res.json({notifications:rows})}catch(error){console.error("Employee notifications load failed:",error);res.status(500).json({message:"Unable to load your notifications"})}}
export async function createNotification(req,res){try{await ensureNotificationsTable();const{type="message",title,message,payload=null}=req.body||{};if(!title||!message)return res.status(400).json({message:"Title and message are required"});const employeeId=Number(req.user?.employeeId);if(!Number.isInteger(employeeId)||employeeId<=0)return res.status(403).json({message:"A valid employee account is required"});const[employeeRows]=await pool.query("SELECT name FROM employees WHERE employee_id = ? LIMIT 1",[employeeId]);const employeeName=employeeRows[0]?.name||String(employeeId);const[result]=await pool.query(`INSERT INTO notifications (type,title,message,employee_id,employee_name,payload) VALUES (?,?,?,?,?,?)`,[type,title,message,employeeId,employeeName,payload?JSON.stringify(payload):null]);res.status(201).json({id:result.insertId,message:"Notification created"})}catch(error){console.error("Notification creation failed:",error);res.status(500).json({message:"Unable to create notification"})}}
export async function markNotificationRead(req,res){try{await ensureNotificationsTable();await pool.query("UPDATE notifications SET is_read = 1 WHERE notification_id = ?",[req.params.id]);res.json({message:"Notification marked as read"})}catch(error){console.error("Notification update failed:",error);res.status(500).json({message:"Unable to update notification"})}}
export async function markAllNotificationsRead(_req,res){try{await ensureNotificationsTable();await pool.query("UPDATE notifications SET is_read = 1 WHERE is_read = 0");res.json({message:"All notifications marked as read"})}catch(error){console.error("Notification update failed:",error);res.status(500).json({message:"Unable to update notifications"})}}
export async function deleteNotification(req,res){try{await ensureNotificationsTable();const[result]=await pool.query("DELETE FROM notifications WHERE notification_id = ?",[req.params.id]);if(!result.affectedRows)return res.status(404).json({message:"Notification not found"});res.json({message:"Notification removed"})}catch(error){console.error("Notification delete failed:",error);res.status(500).json({message:"Unable to remove notification"})}}